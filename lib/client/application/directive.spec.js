
'use strict';

describe('Module "application/directive"', function () {
  var directive = require('application/directive')

  it('should be defined.', function () {
    expect(directive).toBeDefined()
  })

  it('should have a the correct name.', function () {
    //expect(directive.name).toEqual('')
  })

  it('should be rendered', function () {
    //var element = {} //query('#' + ID)
    //console.log(document)
    expect({}).toBeDefined()
  })
})